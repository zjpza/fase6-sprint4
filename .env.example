# Copie para `.env` na raiz do projeto e troque o valor.
# Gere um segredo de 32+ bytes com:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=troque-por-um-segredo-aleatorio-de-32-bytes
